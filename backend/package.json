{
  "name": "number-drop-game-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "deploy": "sam build && sam deploy --guided",
    "local": "sam local start-api"
  },
  "dependencies": {
    "aws-sdk": "^2.1400.0",
    "express": "^4.18.0",
    "cors": "^2.8.5"
  }
}